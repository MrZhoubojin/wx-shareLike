<!--components/nav/index.wxml-->
<view class="container">
    <image class="" src="{{ latest ? disLeftSrc : leftSrc }}"
    bind:tap="onLeft" />
    <text>{{ title }}</text>
    <image class="" src="{{ first ? disRightSrc : rightSrc }}"
    bind:tap="onRight" />
</view>
 